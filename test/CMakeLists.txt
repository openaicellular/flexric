if (UNIT_TEST)
    add_subdirectory(agent-ric-xapp)
    add_subdirectory(agent-ric)
    add_subdirectory(encode_decode)
    add_subdirectory(proxy_ag)
    add_subdirectory(utils)
    add_subdirectory(sm)
    enable_testing()
endif()
